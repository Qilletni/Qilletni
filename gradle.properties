mavenCentralPublishing=true
signAllPublications=true

# Single source of truth for the Qilletni core and API versions, which are always released together.
# See `.qilletni/release.yml` and `RELEASE.md` for the release process.
qilletniVersion=1.0.2
